# GIF to ASCII Art Converter

This script converts GIF images into ASCII art. The conversion is done frame by frame, making it possible to view animated ASCII art in the terminal.

## Usage

To run the script, use the following command:

```bash
pip install requests pillow

python gif2ascii.py
